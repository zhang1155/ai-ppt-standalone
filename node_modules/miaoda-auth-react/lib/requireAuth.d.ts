import { ReactNode } from "react";
interface RequireAuthProps {
    children: ReactNode;
    whiteList?: string[];
}
export declare const RequireAuth: ({ children, whiteList }: RequireAuthProps) => import("react/jsx-runtime").JSX.Element | null;
export {};
